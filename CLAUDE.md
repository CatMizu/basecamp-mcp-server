# basecamp-mcp — implementation in progress

This repository is being rewritten from a Claude Code plugin into a remote MCP
server for Basecamp 3. See the pending commits on branch `mcp-rewrite` for the
scaffold, OAuth wrapper, and v1 tool set.

# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# SQLite vault — never commit
vault.db
vault.db-*

# Fly.io config — gitignored so the production hostname isn't published.
# Copy fly.example.toml to fly.toml and fill in app/volume names.
fly.toml

# Logs
*.log
npm-debug.log*

# OS
.DS_Store

# Coverage
coverage/

# Legacy basecamp plugin config (obsolete but ignored for developer safety)
.basecamp/
